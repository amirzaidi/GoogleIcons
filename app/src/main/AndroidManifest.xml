<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="amirz.googleicons">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_icon_pack"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="com.teslacoilsw.launcher.THEME" />
                <action android:name="com.lineageos.trebuchet.THEME" />
            </intent-filter>
        </activity>
    </application>
</manifest>
